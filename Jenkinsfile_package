// pipeline jenkins package application pour déploiement : Capistrano (aka CR7), php deployer, zip de zouave
pipeline { 
	agent any
	stages {
		stage('package app'){
			steps{
				echo 'package app'
				sh ''
				
			}
		}
	}
}
