# pipeline git clone de l'application, installation lib et dépendances
pipeline {
	agent any
	stages {
		stage('git clone'){
			steps{
				echo 'git clone'
				sh 'cd env/'
				sh 'git clone https://username:password@github.com/RPSM2/MSPR2.git'
				sh 'cd project/'
			}
		}
	}
}
